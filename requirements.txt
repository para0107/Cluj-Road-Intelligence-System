# ─── Core ────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
numpy==1.26.4
pandas==2.2.2
scipy==1.12.0                  # REQUIRED by scikit-learn for DBSCAN distance metrics

# ─── Computer Vision ─────────────────────────────────────────────────────────
opencv-python==4.9.0.80
Pillow==9.5.0
albumentations==1.4.8

# ─── Deep Learning ───────────────────────────────────────────────────────────
# PyTorch — install separately depending on your CUDA version.
# With CUDA 12.1 (RTX 2050):
#   pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu121
torch==2.2.2
torchvision==0.17.2
timm==0.9.16                   # EfficientNet-B3 pretrained weights

# ─── Monodepth2 networks module ──────────────────────────────────────────────
# Required by Monodepth2 networks module at import time
tensorboardX==2.6.2.2

# ─── RT-DETR via Ultralytics ─────────────────────────────────────────────────
ultralytics==8.2.18            # Includes RT-DETR-L support

# ─── Machine Learning ────────────────────────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.4.2            # DBSCAN (Stage 6), preprocessing, metrics
optuna==3.6.1                  # Bayesian hyperparameter optimisation
mealpy==3.0.1                  # Metaheuristics optimization interface

# ─── Database ────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9         # PostgreSQL PostGIS driver (Stage 7)
SQLAlchemy==2.0.30             # ORM database connection engine
GeoAlchemy2==0.15.1            # PostGIS spatial types extension
alembic==1.13.1                # DB migrations tracking

# ─── Geospatial ──────────────────────────────────────────────────────────────
shapely==2.0.4                 # Geometry operations (pothole mask areas)
pyproj==3.6.1                  # Coordinate reference system transforms

# ─── Solar / Sun Angle ───────────────────────────────────────────────────────
pysolar==0.11                  # Sun altitude angles (Stage 1 tracking)

# ─── GPX Parsing ─────────────────────────────────────────────────────────────
gpxpy==1.6.2                   # Parse telemetry .gpx logs (Stage 1)

# ─── HTTP / APIs ─────────────────────────────────────────────────────────────
requests==2.32.2               # Core HTTP communication tracking
httpx==0.27.0                  # Async HTTP client framework

# ─── Backend ─────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ─── Scheduler ───────────────────────────────────────────────────────────────
APScheduler==3.10.4            # Automated pipeline orchestration trigger
pytz==2024.1                   # REQUIRED by APScheduler for Europe/Bucharest TZ lookups

# ─── Utilities ───────────────────────────────────────────────────────────────
tqdm==4.66.4                   # Progress bars
loguru==0.7.2                  # Structured console logging
PyYAML==6.0.1                  # Configurations file parser
python-multipart==0.0.9        # Handles dashboard UI binary file uploads

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6

# ─── Compatibility ───────────────────────────────────────────────────────────
typing-extensions==4.11.0      # Backports for type checking definitions

pytesseract==0.3.10              # OCR for debugging (optional, not used in main pipeline)
easyocr==1.7.0                   # OCR for debugging (optional, not used in main pipeline)