# backend/requirements.backend.txt
#
# Only the packages the FastAPI backend actually imports at runtime.
# Heavy ML / training dependencies (torch, ultralytics, timm, xgboost, …)
# are NOT listed here — they live in requirements.txt for local dev / pipeline work.

# ─── Environment ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── Web framework ───────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
websockets==12.0               # WS protocol for uvicorn — Live mode push channel
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9        # file upload support

# ─── Auth (all free/open-source; passwords hashed with stdlib pbkdf2) ────────
PyJWT==2.8.0                   # JWT issue/verify (HS256)
email-validator==2.1.1         # pydantic EmailStr support

# ─── Database ────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
SQLAlchemy==2.0.30
GeoAlchemy2==0.15.1
alembic==1.13.1

# ─── Geospatial (used in routes/models) ──────────────────────────────────────
shapely==2.0.4
pyproj==3.6.1

# ─── HTTP ────────────────────────────────────────────────────────────────────
requests==2.32.2
httpx==0.27.0

# ─── Utilities ───────────────────────────────────────────────────────────────
loguru==0.7.2
typing-extensions==4.11.0
numpy==1.26.4
pandas==2.2.2
