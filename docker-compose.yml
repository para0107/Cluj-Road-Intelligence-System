services:

  # ── PostgreSQL / PostGIS database ──────────────────────────────────────────
  db:
    image: postgis/postgis:15-3.3
    container_name: cluj_monitor_db
    restart: unless-stopped
    environment:
      POSTGRES_USER:     ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB:       ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      # Auto-create the schema on FIRST start (empty pgdata volume only).
      # Files here run once, inside POSTGRES_DB, before the backend connects.
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout:  5s
      retries:  5

  # ── FastAPI backend ─────────────────────────────────────────────────────────
  backend:
    build:
      context:    .
      dockerfile: backend/Dockerfile
    container_name: cluj_monitor_backend
    restart: unless-stopped
    env_file: .env
    environment:
      # The backend container reaches the DB container by service name, not localhost.
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

      # Container-side mount point for the shared data/ directory.
      PROJECT_DATA_DIR: /app/data

      # ── Auth ────────────────────────────────────────────────────────────
      # SECURITY: no credential defaults live in this file or in code.
      # Set JWT_SECRET and the ADMIN_* trio in .env (git-ignored).
      # - JWT_SECRET unset  → backend generates a random per-process secret
      #   (works, but sessions reset on every restart).
      # - ADMIN_* not all set → no admin is seeded (warning logged).
      JWT_SECRET: ${JWT_SECRET:-}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
      # Optional free Google OAuth client id (console.cloud.google.com);
      # leave empty to hide the Google button in the UI.
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}

    ports:
      - "8000:8000"
    volumes:
      # ── Shared data volume ───────────────────────────────────────────────
      - ./data:/app/data

    depends_on:
      db:
        condition: service_healthy

  # ── React / Vite frontend (served by Nginx) ─────────────────────────────────
  frontend:
    build:
      context:    .
      dockerfile: frontend/Dockerfile
    container_name: cluj_monitor_frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  pgdata:
    driver: local